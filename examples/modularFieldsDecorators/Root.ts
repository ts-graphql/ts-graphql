// If you do this be sure to pass an instance of `Root` to `rootValue` in `graphql`.
// Otherwise if you want to leave that undefined,
// you can leave this out and use `@Extends(undefined)` and `Extension<undefined>` instead
export default class Root {}
