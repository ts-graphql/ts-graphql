import 'jest';
import { exec } from 'child_process';
import { readdir } from 'fs';

describe('Decorator type validation', () => {
  // TODO run tsc on invalid/valid files
  it('passes for valid files', () => {

  });

  it('fails for invalid files', () => {

  });
});
